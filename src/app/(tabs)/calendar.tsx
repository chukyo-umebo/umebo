import { MainTemplate } from "@/components/template/main";

export default function Index() {
    return (
        <MainTemplate
            title="カレンダー"
            subtitle="中京大学の年間スケジュールが書いてあります"
            noOverScroll
        ></MainTemplate>
    );
}
